{
  "name": "mind-assist",
  "version": "0.1.0",
  "private": true,
  "description": "MindAssist AI - Интеллектуальная поддержка для психологов",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run --prefix apps/web dev",
    "build:web": "npm run --prefix apps/web build"
  }
}
